package handler

// chatGuide is a comprehensive user guide embedded into the AI chat system prompt.
// It is maintained as a Go string constant so that the single binary includes it.
const chatGuide = `
## Phaeton 사용 가이드

### 앱 만들기
1. 상단 메뉴에서 "앱" 클릭
2. "새 앱 만들기" 버튼 클릭 (또는 "AI로 만들기")
3. 이름(한글)과 영문 ID(자동변환)를 입력
4. 왼쪽 항목 팔레트에서 항목을 드래그앤드롭으로 폼에 추가
5. 항목 클릭 시 오른쪽에 속성 패널이 열림 (이름, 타입, 필수 여부, 폭/높이 등)
6. "저장" 클릭 → 실제 PostgreSQL 테이블이 생성됨

### AI로 앱 만들기
1. 앱 생성 화면에서 "AI로 만들기" 클릭
2. 만들고 싶은 앱을 자연어로 설명 (예: "출장 신청서를 관리하고 싶습니다")
3. AI가 추가 질문이 있으면 질문 패널이 나타남 → 답변 후 생성
4. AI가 앱 구조(항목 목록)를 제안
5. "적용하기" 클릭 → 빌더에 항목이 채워짐 → 수정 후 저장

### 항목 유형 상세

#### 데이터 항목
| 유형 | 설명 | 주요 옵션 |
|------|------|-----------|
| 텍스트(text) | 한 줄 텍스트 | display_type: url/email/phone, 유효성 검사(이메일/URL/전화/정규식) |
| 긴 글(textarea) | 여러 줄 텍스트 | rows, 높이(height) 2-3 권장 |
| 숫자(number) | 소수점 포함 | display_type: currency(통화)/percent(퍼센트)/progress(진행률) |
| 정수(integer) | 정수만 | 숫자와 동일한 옵션 |
| 체크박스(boolean) | 예/아니오 | 기본값 설정 가능 |
| 날짜(date) | 날짜 선택기 | YYYY-MM-DD, 캘린더 보기 활성화에 필요 |
| 날짜시간(datetime) | 날짜+시간 | YYYY-MM-DD HH:MM |
| 시간(time) | 시간만 | HH:MM |
| 선택(select) | 드롭다운/라디오 | choices 배열, display: dropdown/radio |
| 다중선택(multiselect) | 복수 선택 | choices 배열, 체크박스 표시 |
| 연결(relation) | 다른 앱과 연결 | 대상 앱, 연결 유형(1:1/1:N/N:M), 삭제 시 동작 |
| 사용자(user) | 사용자 선택 | 활성 사용자 목록에서 선택 |
| 파일(file) | 파일 첨부 | 업로드 후 다운로드 링크 표시 |
| json | JSON 데이터 | 자유 형식 JSON 입력 |
| 자동번호(autonumber) | 자동 채번 | prefix, start 설정 가능. 폼에서 읽기 전용 |

#### 고급/수식 항목 (다른 항목의 값을 참조하여 자동 계산)
| 유형 | 설명 | 주요 옵션 |
|------|------|-----------|
| 수식(formula) | 다른 항목 값으로 계산 | expression (예: price * quantity) |
| 참조값(lookup) | 연결된 앱의 항목 값 가져오기 | relation_field, target_field |
| 요약 계산(rollup) | 연결된 데이터의 집계 | relation_field, target_field, function (SUM/COUNT/AVG/MIN/MAX/COUNTA) |

#### 디자인 항목 (데이터 없음, 폼 구조용)
| 유형 | 설명 |
|------|------|
| 제목 표시(label) | 섹션 제목/안내 텍스트 표시 |
| 구분선(line) | 수평 구분선 |
| 여백(spacer) | 수직 빈 공간 |

### 항목 배치
- **폭(width)**: 1(1/6), 2(1/3), 3(1/2), 6(전체). 한 줄에 폭 합이 6이 되도록 배치
  - 예: 폭 3인 항목 2개 → 나란히 배치
  - 예: 폭 2인 항목 3개 → 한 줄에 3칸
- **높이(height)**: 1, 2, 3줄. 긴 글은 2-3 권장
- 디자인 항목(제목 표시/구분선/여백)은 항상 전체 폭

### 데이터 입력/수정/삭제
- **새 데이터**: 앱 화면에서 "새 데이터" 클릭 → 폼 작성 → 저장
- **수정**: 행 클릭 → 사이드 패널에서 "편집" 탭 → 수정 후 저장
- **인라인 수정**: 테이블 셀을 직접 클릭해서 수정 (연결 항목 제외)
- **삭제**: Actions 열의 삭제 버튼 (관리자/운영자만 가능)
- **검색**: 상단 검색창에 입력 (텍스트/긴 글 항목에서 전문 검색)

### 보기(View) 종류

#### 리스트 보기 (기본)
- 테이블 형식으로 데이터 표시
- 기능: 정렬(다중 항목), 필터(항목별 조건), 페이지네이션(10/20/50/100건), 컬럼 표시/숨기기, 컬럼 고정(왼쪽/오른쪽)
- 숫자 항목은 하단에 합계/평균 표시

#### 보드 보기
- 카드형 보드, 드래그앤드롭으로 상태 변경
- 선택(select) 항목의 선택지 또는 프로세스 상태를 열(column)로 사용
- 카드 클릭 시 상세 보기

#### 캘린더 보기
- 월별 달력에 데이터 표시
- 날짜(date/datetime) 항목이 있어야 활성화
- 월 이동, 오늘 바로가기

#### 갤러리 보기
- 이미지/파일 기반 그리드 레이아웃

#### 보기 저장
- 현재 필터+정렬 설정을 이름 붙여 저장 가능
- 저장된 보기 간 전환

### 프로세스(워크플로) 설정
1. 앱 설정 → "프로세스 설정"
2. "상태 워크플로 사용" 체크
3. 상태 추가 (이름 입력 → Enter)
4. 상태별 색상 설정 (회색/파랑/초록/노랑/주황/빨강/보라/청록)
5. 상태 이동(Transition) 설정: 어떤 상태에서 어떤 상태로 이동 가능한지
6. 데이터 입력 시 현재 상태가 표시되고, 가능한 상태 이동 버튼이 나타남
7. 보드 보기에서 드래그로 상태 변경 가능

### 필터 연산자
| 항목 유형 | 사용 가능 연산자 |
|-----------|------------------|
| 텍스트/긴 글 | 같음, 다름, 포함, 비어있음 |
| 숫자/정수/날짜/시간 | 같음, 다름, 초과, 이상, 미만, 이하, 비어있음 |
| 체크박스 | 같음, 다름, 비어있음 |
| 선택 | 같음, 다름, 포함(in), 비어있음 |
| 다중선택 | 포함, 비어있음 |

### 앱 템플릿 내보내기/가져오기
- **내보내기**: 앱 설정 → "내보내기" → 앱 구조(항목, 보기, 프로세스, 자동화, 차트)를 JSON 템플릿으로 다운로드
- **가져오기**: 앱 생성 화면 → "템플릿으로 만들기" → JSON 템플릿 파일 선택 → 동일한 구조의 새 앱 생성
- 데이터는 포함되지 않으며, 앱 구조만 복제됨

### CSV 내보내기/가져오기
- **내보내기**: 앱 화면 → "내보내기" → 현재 필터/정렬 조건의 전체 데이터를 CSV로 다운로드
- **가져오기**: 앱 화면 → "가져오기" → CSV 파일 선택 → 헤더가 항목 영문 ID/라벨과 매칭되어 대량 등록

### 댓글
- 데이터 상세 패널 → "댓글" 탭
- 댓글 작성/삭제 (자기 댓글만 삭제 가능)
- 댓글 작성 시 다른 사용자에게 알림

### 알림
- 상단 벨 아이콘에 읽지 않은 알림 수 표시
- 알림 종류: 댓글 알림, 상태 변경 알림, 데이터 수정 알림
- 클릭하면 읽음 처리, "모두 읽음" 버튼

### 변경 이력
- 데이터 상세 패널 → "이력" 탭: 해당 데이터의 모든 변경 내역 (누가/언제/무엇을)
- 관리 메뉴 → "이력": 구조 변경 이력
  - 작업 유형, 안전 등급(SAFE/CAUTIOUS/DANGEROUS), 실행 내역 미리보기
  - 되돌리기 기능 (주의: 데이터 손실 가능)

### 사용자 관리 (관리자만 접근 가능)
- 사용자 목록: 이름, 이메일, 역할, 부서, 직위, 활성 상태
- 사용자 생성/수정, 활성/비활성 전환
- **역할(Role)**:
  - Director(관리자): 모든 권한
  - PM(운영자): 앱 생성/편집, 데이터 CRUD
  - Engineer(담당자): 데이터 CRUD
  - Viewer(열람자): 읽기만 가능

### 앱별 멤버 관리
- 앱 설정 → "멤버" 탭
- 사용자별 역할 부여: Owner(소유자), Editor(편집자), Viewer(열람자)
- 앱별 접근 권한과 조직 역할은 별도 (둘 다 만족해야 접근 가능)

### 조직도 (관리자만 접근 가능)
- 부서 계층 구조 관리 (트리 형태)
- 부서 생성/수정/삭제/순서 변경
- 사용자별 부서 배정

### 자주 묻는 질문

**Q: 영문 ID가 뭔가요?**
A: 영문 식별자입니다. 한번 생성하면 변경할 수 없으며, API URL과 DB 테이블명에 사용됩니다. 소문자+숫자+밑줄만 가능합니다.

**Q: 항목을 삭제하면 데이터가 사라지나요?**
A: 네, 해당 항목의 모든 데이터가 삭제됩니다. 확인 대화상자가 나타나며, 위험도(SAFE/CAUTIOUS/DANGEROUS)가 표시됩니다.

**Q: 보드 보기가 안 보여요.**
A: 선택(select) 타입 항목이 있어야 합니다. 프로세스가 활성화되어 있으면 상태 항목 기준으로도 가능합니다.

**Q: 캘린더 보기가 안 보여요.**
A: 날짜(date) 또는 날짜시간(datetime) 타입 항목이 있어야 합니다.

**Q: 연결(relation) 항목은 어떻게 설정하나요?**
A: 항목 추가 시 "연결" 타입 선택 → 대상 앱 선택 → 연결 유형(1:1, 1:N, N:M) 선택 → 삭제 시 동작 설정

**Q: 데이터를 대량으로 넣고 싶어요.**
A: CSV 가져오기를 사용하세요. 앱 화면 → "가져오기" → CSV 파일 선택. 헤더가 항목 영문 ID와 일치해야 합니다.

**Q: 삭제한 데이터를 복구할 수 있나요?**
A: 삭제된 데이터는 휴지통에서 복구 가능합니다.

**Q: AI로 만들기에서 오류가 나요.**
A: AI 서버(vLLM)가 실행 중인지 확인하세요. 설명을 더 구체적으로 작성하면 정확도가 올라갑니다.

**Q: 권한이 없다고 나와요.**
A: 관리자(Director)에게 해당 앱의 멤버로 추가해달라고 요청하세요. 조직 역할과 앱별 멤버 권한 모두 필요합니다.

### 자동화(Automation)
자동화는 특정 이벤트 발생 시 자동으로 동작을 실행하는 규칙입니다.

#### 자동화 만들기
1. 앱 설정 → "자동화" 메뉴
2. "새 자동화" 또는 "AI로 만들기" 클릭
3. 이름, 트리거, 조건, 액션을 설정
4. 저장하면 즉시 활성화

#### 트리거 종류
| 트리거 | 설명 |
|--------|------|
| 데이터 생성 | 새 데이터가 생성될 때 |
| 데이터 수정 | 기존 데이터가 수정될 때 |
| 데이터 삭제 | 데이터가 삭제될 때 |
| 상태 변경 | 프로세스 상태가 이동될 때 (from/to 상태 지정 가능) |

#### 조건
트리거 발생 후 특정 조건을 만족할 때만 액션 실행. 여러 조건은 AND(모두 만족) 논리.
- 연산자: 같음, 같지 않음, 포함, 초과, 미만, 비어있음, 비어있지 않음

#### 액션 종류
| 액션 | 설명 | 설정 항목 |
|------|------|-----------|
| 알림 발송 | 인앱 알림 전송 | 수신자(작성자/특정사용자/항목참조), 제목, 내용 |
| 항목 값 업데이트 | 데이터의 항목 값을 자동 변경 | 대상 항목, 값 |
| Webhook 호출 | 외부 URL에 HTTP 요청 | URL, 헤더 |

#### 실행 로그
각 자동화의 실행 이력을 확인할 수 있습니다 (성공/실패/건너뜀, 소요시간).

**Q: 자동화는 누가 만들 수 있나요?**
A: 관리자(Director)와 운영자(PM) 역할만 가능합니다.

**Q: 자동화가 실행되지 않아요.**
A: 자동화가 "활성화" 상태인지 확인하세요. 또한 조건이 올바르게 설정되었는지 항목 영문 ID와 값을 확인하세요.
`
